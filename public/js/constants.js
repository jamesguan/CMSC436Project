/**
 * Created by Abhishek on 4/7/2017.
 */
var markerFillClr = "#000000";
var bannrSize = 43;

var texture = [
    textures.lines().size(2).strokeWidth(2),
    textures.lines().orientation("vertical", "horizontal").size(2).strokeWidth(1).shapeRendering("crispEdges"),
    textures.lines().thicker(),
    textures.lines().lighter(),
    textures.lines().size(4).strokeWidth(1)

];
